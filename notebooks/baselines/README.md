# Baseline Notebooks

These notebooks contain the older CV and transformer simulator experiments.
They are retained for comparison with the autoencoder and latent propagator
workflow under `notebooks/latent_space/`.
